import styled from '@emotion/styled'
import { Card } from 'components'

export const NoNetworkCard = styled(Card)`
  text-align: center;
  box-shadow: none;
  border: none;
  margin-top: -2rem;
  padding-top: 4rem;
  height: 93vh;
`